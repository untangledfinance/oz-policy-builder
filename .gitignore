node_modules
dist
dist-cjs
.env
# per-run demo deployment, regenerated by demo-gates-testnet.ts setup
scripts/.demo-state.json
# compiled demo CLI; rebuild with `bun run prime:build`
/prime
*.log
# ...except the audit evidence pack: these logs ARE the audit artifact and must
# travel with the repo. Regenerate them whenever a claim they back changes.
!docs/audit/evidence/*.log
.superpowers/
.worktrees/
.docusaurus/
.turbo/
.cache/
coverage/
*.tsbuildinfo
worker-configuration.d.ts
*.tgz
packages/policy-builder-cli/test/fixtures/
packages/policy-builder-cli/test/.tmp/

# Rust build artifacts
target/

# Working artifacts: specs, plans and reports are not repo content.
plans/
specs/
docs/superpowers/

# Disposable testnet keys for the scenario fixtures. Secrets: never commit.
scripts/.scenario-state.json
