// src/ir/index.ts - re-export the PolicyIR ("Policy Tree") types.

export type {
  IRCompare,
  IRCompOp,
  IRCondition,
  IRLogic,
  IRPolicyRule,
  IRScalarType,
  IRSelector,
  IRVecMode,
  PolicyIR,
} from './types.ts'
